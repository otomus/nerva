/**
 * Nerva Protocol Definitions
 *
 * Canonical TypeSpec contracts for all Nerva primitives.
 * Each file defines the models, enums, and interface contracts
 * that all language implementations (Python, Node.js, etc.) must follow.
 *
 * Generated JSON Schema output is the source of truth for conformance testing.
 */

import "./exec_context.tsp";
import "./router.tsp";
import "./runtime.tsp";
import "./tools.tsp";
import "./memory.tsp";
import "./responder.tsp";
import "./registry.tsp";
import "./policy.tsp";
import "./orchestrator.tsp";
import "./middleware.tsp";
